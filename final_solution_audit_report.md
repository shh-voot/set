# 华为杯 D 题最终方案审查报告

## 1. 审查信息

- 审查日期：2026-09-24
- 审查对象：本地仓库与 GitHub `shh-voot/set` 的同步版本
- 当前分支：`main`
- 当前提交：`47d9bf4 添加赛题理解文档集`
- 审查范围：赛题原文、基础数据、求解脚本、结果工作簿、最终结果核验表
- 审查目的：核对数据一致性、赛题约束符合性、结果可复现性和学术表述规范

## 2. 总体结论

当前仓库不能支持“4 个问题全部符合赛题要求”“结果已 100% 验证”或“数据可直接用于论文定稿”的结论。

审查没有发现直接篡改原始附件数据的证据，因此不能据此认定存在故意伪造数据。但仓库存在结果文件、核验表和求解代码互相矛盾，部分结果使用简化模型，部分约束没有被实际验证的问题。若将这些结果直接写入论文，会构成对结果可靠性和符合性的过度表述。

## 3. 赛题基准事实

根据赛题原文和附件：

1. 场景包含 15 个服务区和 80 个不可拆货箱；每个货箱只能安排一次。
2. 运输无人机实体库存为 A 型 4 架、B 型 2 架、C 型 2 架。
3. 运输无人机共享电池库存为 A 型 6 组、B 型 4 组、C 型 4 组；等效完全充电时间分别为 1800 s、2400 s、3000 s，即 30 min、40 min、50 min。
4. 中继无人机实体库存为 R01、R02 两架，中继能源组件库存为 6 组。
5. 问题一要求完成全部货箱配送；问题二要求验证货箱时限、实体无人机、共享电池和充电周转；问题三要求运输和中继连续通信，且中继链路不允许多跳；问题四要求基于问题三方案进行 2 组和 3 组独立分区，资源不得跨组调配。
6. 运输航段和通信链路均应按题目规定的 30 m DEM 及相应的高度、能耗、链路和双向判定规则计算。

## 4. 关键审查发现

### P0-1：问题一权威结果仍是 49 箱，违反完整配送要求

权威核验表和 `结果/问题一_配送方案.xlsx` 仍使用以下数据：

- 15 个服务区；
- 49 件货箱；
- 45.90 kWh；
- B 型 14 架次、C 型 1 架次。

但赛题共有 80 个货箱，因此该文件不能作为问题一最终答案。

`结果/问题一_18架次方案.xlsx` 和 `结果/问题一_超级优化方案.xlsx` 经逐箱编号检查后包含 80 个唯一货箱编号，没有缺失、重复或额外编号。这两个文件可以作为候选完整配送方案，但其计算仍使用简化的水平距离和载荷修正能耗，尚未按 DEM 航段最高地面高程、服务区作业高度、巡航高度、爬升和下降规则重算。因此不能直接把其中能耗称为符合题目统一模型的最终能耗。

其中名为“18架次”的工作簿实际包含 19 条架次记录，文件名与内容不一致；“18架次”不能作为已达到的架次指标。

证据文件：

- `结果/问题一_配送方案.xlsx`
- `结果/问题一_18架次方案.xlsx`
- `结果/问题一_超级优化方案.xlsx`
- `solve_problem1_18trips.py`

### P0-2：问题一能耗模型没有使用载荷参数和 DEM 航段高度

`src/energy/energy_model.py` 接收 `payload_kg` 参数，但当前能耗公式没有使用该参数。该模型只按起终点高度差和水平距离计算，不能代表题目要求的“航段最高 DEM 地面高程以上 50 m”的巡航过程。

问题一脚本 `solve_problem1_18trips.py` 使用 `calculate_energy` 的经验修正项，而非题目规定的分段能耗模型。论文中不得把该结果写成严格按附录 2 计算的能耗。

### P0-3：问题二结果、代码和基础库存不一致

结果文件 `结果/问题二_符合赛题规则方案.xlsx` 显示：

- 33 架次；
- 80 箱；
- 完成时间 126.39 min；
- 总能耗 31.16 kWh；
- 8 架运输无人机；
- 使用电池 14 组。

结果中的机型数量和电池数量与附件库存 A4、B2、C2 以及 A6、B4、C4 一致。但当前脚本 `solve_problem2_compliant.py` 设置为 B 型 6 架、A/C 各 2 架，并初始化 B 型 6 组电池、A/C 各 4 组电池。因此当前结果不能由当前脚本直接复现。

此外，脚本在 `calculate_mission_time_energy` 中明确使用平均单点 15 min、平均能耗 0.8 kWh，并注明尚未调用地形分析和真实能耗模型。结果文件只有架次级货箱数量，没有货箱编号、实际交付时刻、截止时间和按时标记。因此“80/80 按时送达”没有可审计证据。

### P0-4：充电时间读取错误，核验表内部也互相矛盾

基础数据的共享电池库存给出：

| 机型 | 等效完全充电时间 |
|---|---:|
| A | 30 min |
| B | 40 min |
| C | 50 min |

但 `src/data/data_loader.py` 将运输无人机装载或交接相关列误读为完全充电时间，当前脚本得到 A/B 30 min、C 36 min。最终核验表前文也写成 A30、B30、C36，后文又写成 A30、B40、C50，说明充电口径尚未统一。

问题二的 126.39 min 不能在充电时间字段未修正、代码和结果未重新生成前作为最终完成时间。

### P0-5：问题三结果实际不是 100% 覆盖

当前 `结果/问题三_中继部署方案_新.xlsx` 实际内容为：

- 中继部署表包含 R01-R06，共 6 架；
- 覆盖详情中 S008 的状态为“否”，其余 14 个服务区为“是”；
- 中继总能耗约 15.02 kWh；
- 链路余量表没有证明 S008、S012 的基站直连链路。

这与核验表中“5 架中继、S008/S012 基站直连、100% 覆盖、30.69 kWh”直接矛盾。当前提交说明还写有“中继总数 6→8”，但实际结果文件是 6 架，说明最新提交的说明、结果和核验表没有统一。

### P0-6：问题三超过中继实体库存，且没有实现多时段复用

附件只提供 R01、R02 两架中继无人机。当前结果使用 6 架中继，超过实体库存；代码也按多个中继同时部署处理，没有给出 R01、R02 的多时段复用排程。因此不能把 6 架中继方案视为资源可行方案。

### P0-7：问题三通信判定没有真正使用 DEM LoS 和严格双向预算

`solve_problem3_relay.py` 的链路判定使用三维距离、Friis 公式和固定障碍损耗，未调用 `src/communication/los_model.py` 的 DEM 采样来判断沿线遮挡。

代码的每条链路调用只传入一个发射端和一个接收端参数，不能证明两个方向分别满足题目规定的双向链路预算。结果中出现的链路余量也不能替代 DEM LoS 证据。

### P0-8：问题三中继能耗没有完整覆盖题目规则

`calculate_relay_energy` 只计算往返巡航能耗、悬停能耗和通信附加能耗，未完整计入：

- 爬升和下降阶段；
- 固定准备时间；
- 建链时间；
- 架次周转时间；
- 返航电量下限；
- 中继能源组件 SOC 和充电周转。

因此 15.02 kWh、30.69 kWh 或其他中继能耗数字都不能在当前实现下作为严格最终值。

### P0-9：问题四不是符合题意的独立分区资源仿真

当前 `solve_problem4_fixed.py` 存在以下问题：

1. 只统计 B/C 型运输任务，没有统计 A 型运输无人机及其电池。
2. 按总能耗乘安全系数估算电池，不跟踪每组电池 SOC、任务占用、充电队列和任务时序。
3. 充电桩数量采用 30 小时平均负载估算，没有进行离散事件仿真。
4. 中继资源按 `中继数量 × 2` 组电池估算，属于未经附件支持的假设。
5. 中继资源在代码中作为所有批次共享，违反题目“资源不得跨组调配”。
6. 结果文件没有列出每组 A/B/C 运输无人机、共享电池、中继无人机、中继能源组件和充电桩的库存缺口。

因此 `结果/问题四_2批次_修复版.xlsx` 和 `结果/问题四_3批次_修复版.xlsx` 只能视为初步分区草案，不能称为完整资源配置方案。

## 5. 数据一致性问题

当前仓库至少存在以下互相矛盾的数字：

| 项目 | 文件或说明中的版本 | 实际审查结果 |
|---|---|---|
| 问题一货箱数 | 49 箱 | 题目为 80 箱；候选完整文件为 80 箱 |
| 问题二 B 型无人机数 | 代码 6 架 | 附件和结果应为 2 架 |
| 问题二充电时间 | 核验表出现 30/36 min 和 40/50 min 两套口径 | 附件为 A30、B40、C50 min |
| 问题三中继数量 | 核验表 5 架；提交说明 8 架 | 结果工作簿 6 架；附件实体库存 2 架 |
| 问题三覆盖率 | 核验表 100% | 结果工作簿 S008 为“否”，实际 14/15 |
| 问题三中继能耗 | 核验表 30.69 kWh | 结果工作簿合计约 15.02 kWh |
| 问题四资源 | 核验表 25/26 组电池、4/6 个充电桩等多套数字 | 结果工作簿和代码未完成完整独立资源核算 |

## 6. 学术规范判断

### 6.1 未发现直接伪造原始数据证据

基础数据文件能够读取，且本次审查未发现原始附件被直接改写的证据。审查结论应使用“尚未发现直接伪造证据”，不能写成“已证明不存在任何学术不端”。

### 6.2 当前不当表述

以下表述在现有证据下不应写入论文或提交说明：

- “四个问题全部符合赛题要求”；
- “数据 100% 真实”；
- “问题三实现 100% 通信覆盖”；
- “所有时限约束满足”；
- “全面碾压竞争对手”；
- “符合性 100%”。

167 min 和 59.24 kWh 的外部基准没有在当前仓库中形成同数据、同约束、同评价口径下的可复现实验。论文中应改为“参考基准方案”，并注明来源、输入、约束和计算过程；在无法复现前，不应使用“竞争对手”或“全面碾压”等宣传式表述。

## 7. 修复优先级和验收标准

### P0：提交论文前必须完成

1. 指定唯一的问题一权威文件，完成 80 箱逐箱核对，并按 DEM 和载荷能耗模型重算。
2. 修正充电时间读取：A30 min、B40 min、C50 min；重新生成问题二和问题四结果。
3. 让问题二代码和结果完全一致，输出货箱编号、架次、服务区、实际交付时刻、截止时间、是否按时。
4. 对问题二逐箱验证医疗物资期望时间和首批保障截止时间，输出迟到箱数为 0 的证据，或如实报告迟到情况。
5. 问题三只使用 R01、R02；如需复用，输出中继时段、服务时段、能源组件编号和充电时段。
6. 实现 30 m DEM LoS、两端点双向链路预算、直连/中继状态和连续时间覆盖检查。
7. 问题四实现 A/B/C 运输资源、中继资源、SOC、充电队列和独立分区的离散事件仿真。

### P1：论文定稿前完成

1. 清理或明确标记旧版 49 箱、旧版 69.79 min、旧版 1 架中继等结果。
2. 每个结果文件注明生成脚本、生成时间、输入文件哈希、代码提交号和校验摘要。
3. 统一核验表、README、审查报告、结果工作簿和论文中的数字。
4. 对 2 批次和 3 批次分别列出资源需求、现有库存、缺口和缺口原因。

### 通过审查的最低条件

- 四个问题的结果均能由仓库中的当前代码在固定输入上重新生成；
- 结果文件中的每个货箱、每个架次、每个资源和每个时间约束均可追溯；
- 论文中的能耗、完成时间、覆盖率和资源数量与结果文件逐项一致；
- 所有未验证或采用简化假设的内容在论文中明确标注适用范围。

## 8. 当前论文可使用的数据边界

在上述修复完成前，论文最多可以描述：

- 附件包含 15 个服务区和 80 个货箱；
- 已经生成了一个包含 80 个唯一货箱的候选问题一组批方案；
- 已经生成了一个 33 架次、80 箱的候选问题二调度表；
- 已经生成了问题三和问题四的初步候选结果。

不能把这些候选结果写成“已严格满足全部题目约束的最终最优方案”。
