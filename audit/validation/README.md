# 交接包复核记录

2026-09-26（Asia/Shanghai）使用新统一入口执行：

```shell
python audit/review.py run --round all --full
```

使用独立 Conda 审计环境及 `audit/requirements.txt` 所列版本；两轮输入来自 `git archive` 导出的固定提交。未从当前工作区复制业务源码替代历史源码。

| 轮次 | 固定提交 | 入口运行结果 | 独立约束反例 |
|---|---|---|---|
| 1 | ea5e0fb | 检查器、原主链复现器、针对性检查器退出码均为0 | Q2 45架次；Q3实际超载0；四个投送点断连；两中继超限；Q4迟到28/28 |
| 2 | e94e1cc | 检查器、主链复现器、扩展实验、针对性检查、新实验检查器退出码均为0 | Q2 43架次；Q3实际超载3；四个投送点断连；两中继超限；Q4迟到23/25；CP合成反例重现 |

**这里的退出码0代表审计入口成功完成、确认了已记录问题。** 它不表示原解题方案可行。被审查子程序的预期错误（如交付Q1路径错误）由复现器记录，没有被当作审计器故障。

- [两轮运行摘要](run_summary.json)：命令、提交、预期与实际关键计数。
- `round1/`、`round2/`：统一入口每步产生的原始执行日志。
- 原求解器及 ALNS 的细分日志保存在各轮 `evidence/`；本次完整副本可由统一入口随时重建。

为使摘要可跨机器浏览，`run_dir` 改为相对于审计目录的路径；日志中若出现原本机路径，仅为历史执行记录，不作为复核依赖。
