# 最新提交补充评估报告

审查版本：`e94e1cc`（提交说明：“提交当前解题与论文正式版本”）  
对照版本：`ea5e0fb`；审查日期：2026-09-25 至 2026-09-26  
范围：最新正式交付、新增论文版本、ALNS 实验、NSGA-II／CP-SAT 实验及上一轮问题的回归核查。

## 1. 更新及本地冲突处理

已执行 `fetch` 与 `pull --ff-only origin main`，main 从 `ea5e0fb` 快进到 `e94e1cc`。拉取前本地只有未跟踪的审计目录，没有已跟踪文件修改。

逐路径检查发现，远程新增文件与上一轮 `audit/20260925/` **无路径冲突**，因此保留原目录即可，无需丢弃、覆盖或人工合并。上一轮 109 个审计及复现文件已记录哈希；本轮结束时再次验证全部保持一致。本次补充评估放在独立的 `audit/20260925_update/`。

按 Git 重命名识别统计，新提交包含新增 118、修改 39、删除 60、内容不变的重命名 56 项。许多变化是旧资料归档和图表清理。**原题、基础数据、DEM、`src/` 公共模块和结果提交模板均未改变。** 当前有 408 个受跟踪文件，本次评估没有修改其内容。

环境沿用自建 Conda 前缀 `.audit-env`，为新增 CP-SAT 检查另安装 OR-Tools 9.10.4067；完整版本见 `requirements-audit.txt`。程序运行仅发生在审计复制目录中，未覆盖团队结果，也未进行业务修复。

## 2. 总体结论

**新版本有实际进展，但尚未解决上一轮的主要可行性问题，并出现了新的实际超载和版本一致性问题。仍不能认定为满足全部题目要求的正式终版。**

### 有证据支持的进展

- 问题二正式工作簿从 45 架次降到 **43 架次**；现交付代码可复现该工作簿。
- 新增 ALNS 实验具有真实的破坏、修复及自适应权重更新，其完成时间改善在现有模型下可复现。
- 问题四文字已明确：独立分区有资源缺口，错峰只作为扩展场景。这修正了部分题意表达。
- 问题四独立表已不再混入上一轮的 49 架次；现在为 43 架次，但仍使用另一版问题二数据，且未继承问题三。
- 新 Word 论文补入了有作者、题名、年份和标识符的 7 条参考文献；另有公共公式编号与符号章节，文档完整性有所改善。
- 新增问题一载质量/容积下界讨论。独立按各区需求、最大载质量 80 kg 与容积 0.25 m³ 求下界，总和确为 **18**；这支持架次数下界的算术结果，但仍需证明实际方案满足其他物理约束。

### 需要优先关注的结论

1. **最新问题三交付表有 3 个架次同时超重、超容积。** 原先只是检查入口缺项的风险，现已成为实际违规。
2. **4 个服务区实际交接点断连，两架中继单组件服务能耗超限，均仍存在。**
3. **航程指数只在新论文中改成正确表达式，运行代码仍为线性。** 能耗子公式来源和 DEM 取高问题未解决。
4. **问题二论文数字与工作簿不一致，问题三、四也无法由当前上游结果复现为交付表。**
5. **问题四两种错峰方案分别有 23、25 箱硬截止迟到，原表 `on_time` 仍全为 True。**
6. 新增 NSGA-II／CP-SAT 目录是存在实质缺陷的实验实现；唯一帕累托结果工作簿为空，尚无完整优化成果可验收。

## 3. 上一轮问题的状态更新

| 上轮问题 | 最新状态 | 本轮证据 |
|---|---|---|
| 航程应为 3/2 次幂，代码却线性 | **未修复；论文与代码分歧扩大** | `src/energy/energy_model.py:141` 未变，新章节声称严格采用 3/2 |
| 水平与爬升能耗来源不闭合 | **未解决** | 原题仍未展开子公式；新文写势能公式，运行仍用 `P×η×t` |
| 返航余量进入能耗标定，敏感性抵消 | **未修复** | 同任务 rho 从 0.1 到 0.4，E/上限仍恒为 0.867287 |
| DEM 半像元偏移及非逐像元最大值 | **未修复** | 同一像元中心仍差 4.496735 m；最大低估 7.529427 m |
| Q1 缺 45 项最大安全载荷与余量敏感性结果 | **未补足** | 新增描述和下界，未见对应完整计算交付 |
| Q2 仅单服务区候选、无全局最优证明 | **正式解仍如此；边界说明改善** | ALNS 只改已有架次顺序；NSGA 多点实验尚不可靠 |
| Q3 真实投送高度不一致、连续通信不成立 | **未修复** | S003/S004/S008/S014 仍断连 |
| Q3 单组件超时、无可执行返航换电/接替 | **未修复** | 两架仅服务能耗仍分别超过 2.56 kWh |
| 公共可行性函数未检查载重/容积 | **恶化为实际违规** | 最新 Q3 有 3 个架次超限；干净重跑仍出现 2 个 |
| Q4 读取 Q2，未继承 Q3 | **未修复** | 交付代码仍读取 `问题二_增强优化版.xlsx` |
| Q4 49 架次旧结果 | **旧症状消失，版本问题仍在** | 改为 43 架次，但总能耗对应 93.853962 的 Q2 旧快照，而当前 Q2 为 95.204176 |
| Q4 用错峰替代独立分区 | **文字边界部分修正** | README 承认库存缺口；错峰扩展本身仍不满足时限 |
| Q4 错峰迟到但保存为按时 | **未修复** | 最新为 23/25 箱，替代上轮 28/28 的计数 |
| 根目录与交付目录代码及说明混用 | **版本分歧加重** | README 根入口仍运行 45 架次旧代码，交付目录入口运行 43 架次 |
| 文献书目信息不全、公式无逐项引用 | **部分改善** | Word 书目补齐 7 条，逐式来源及程序映射未闭环 |
| 提交模板未填、Q1 图指标写死 | **未修复** | 模板未变；终稿图脚本仍硬编码 56.5159 |

上一轮通过的检查也重新执行：Q1 的 18 架次、Q2 的 43 架次仍覆盖全部 80 箱且无重复、遗漏、超载、超容积；Q2 原表硬时限、实体机和电池占用检查通过。正确的两阶段充电模块依旧通过。

## 4. 最新正式交付的关键发现

### 4.1 新增 P0：问题三实际超载、超容积

以最新 `解题-gpt/结果/问题三_运输调度_修复版.xlsx` 为对象，直接汇总附件逐箱重量与体积：

| 架次 | 服务区 | 机型 | 实际质量 / 上限 kg | 实际体积 / 上限 m³ |
|---:|---|---|---:|---:|
| 1 | S001 | B | **76 / 30** | **0.159 / 0.073** |
| 2 | S001 | A | **42 / 25** | **0.081 / 0.060** |
| 4 | S006 | A | **45 / 25** | **0.093 / 0.060** |

原因链条明确：[问题三改机型过程](https://github.com/shh-voot/set/blob/e94e1cc70b6f342282eaa9e7d2b15aa84065e381/%E8%A7%A3%E9%A2%98-gpt/%E4%BB%A3%E7%A0%81/solve_problem3_relay.py#L190) 试图减少迟到，只调用 `direct_mission()['feasible']` 筛选机型；而该布尔量仅检查能量与额外航程限制，没有质量/容积。后续重排于是允许用小机型承接原来的大批货箱。

这不是结果表字段显示错误：按原始货箱清单独立求和后同样超限。最新 JSON 的 `feasible_100_percent=true` 因而不成立。将整条 Q2→Q3 链在复制目录重新运行，仍出现 **76 kg 分配给 A 型、42 kg 分配给 B 型**的两条违规，表明不能靠简单重跑消除问题。

详见 `evidence/q3_mission_check.csv`、`evidence/new_candidate_checks.csv`。

### 4.2 仍存在：真实投送点通信失败

最新中继位置与上轮相同。按原题服务区地面以上 30 m、正确双向接口预算进行复核，即使乐观假定两架中继始终在线，以下服务区仍没有可用的直连或中继路线：

| 服务区 | 直连门限余量 dB | R01 路线余量 dB | R02 路线余量 dB |
|---|---:|---:|---:|
| S003 | −5.277 | −6.322 | −14.916 |
| S004 | −5.836 | −9.892 | −9.506 |
| S008 | −6.235 | −8.919 | −12.116 |
| S014 | −2.726 | −12.426 | −5.548 |

负数表示不足；中继列取接入、回传的较差者。该结论再次用正确像元中心双线性视线与原 LOS 工具在真实高度下交叉验证。它是实际交接点反例，并非以少量采样点证明全时域可行。

### 4.3 仍存在：两架中继无法用单组件连续服务到表定结束

中继单组件最多允许耗能 **3.2×0.8=2.56 kWh**。悬停与通信合计 **1.10 kW**，不计往返也最多服务 **139.636 min**。

| 最新交付 | 服务时长 min | 仅服务能耗 kWh | 单组件限额 kWh |
|---|---:|---:|---:|
| R01 | 173.028 | **3.172182** | 2.56 |
| R02 | 155.220 | **2.845702** | 2.56 |

当前组件能量分块仍未落实返航换电、准备建链与不中断接替；两架的组件仍分别写成相同 `RE-01,RE-02`，没有全局身份映射。中继三阶段飞行和 DEM 净空计算也未修正。上述单组件超限结论不依赖缺失的运输爬升子公式。

### 4.4 新增 P1：论文、工作簿、当前代码形成不同结果链

| 指标 | 最新文字/交付记录 | 本轮读取或干净复现 |
|---|---|---|
| Q1 总能耗 | 重排 Word/新章节写 **54.1754**；主表与旧正文仍为 **56.515853** | 当前代码为 **56.515853**；仅修正指数的隔离对照为 **54.181368** |
| Q2 完成时间 / 总能耗 | README 与论文：**171.584004 min / 93.853962 kWh** | 最新正式工作簿及交付代码：**173.727403 min / 95.204176 kWh** |
| Q3 联合完成时间 / 总能耗 | 交付 JSON：**189.175200 min / 99.354617 kWh** | 继承当前 Q2 重跑：**198.977484 min / 101.324454 kWh** |
| Q4 两组错峰完成时间 | **274.699246 min** | 当前链重跑：**296.889364 min** |
| Q4 三组错峰完成时间 | **384.034965 min** | 当前链重跑：**428.860546 min** |

Q3、Q4 共 9 个工作表内容不能复现为所交付内容。这是实际任务和资源数据差异，不是 Excel 压缩包时间戳差异。Q4 独立表各组运输能耗之和为 **93.853962 kWh**，与旧 Q2 文字数值相符，既不是当前 Q2 的 95.204176，也不是当前 Q3 的 92.944613。不能把“都为 43 架次”视为版本一致。

根 README 指示的旧入口 `solve_problem2_lns.py` 仍产生 **45 架次、173.638782 min、97.652608 kWh**。交付目录的新入口才产生 43 架次。本轮按完整目录结构运行新代码，并通过 `PYTHONPATH` 指向复制仓库根目录以满足 `src` 导入。交付 Q1 的数据路径仍指向 `代码/数据`，直接运行时因找不到 DEM 报 `StopIteration`；Q2–Q4 则已改用项目根的数据路径。

**不能选择其中一组较好看的数字作为正式结论；需要来源一致的输入、代码、参数和全链结果。** 本次只记录差异，没有替团队决定使用哪个候选或覆盖结果。

### 4.5 问题四：题意表述改善，实际时限和继承仍不合规

新 README 正确声明独立分区有缺口、错峰只是扩展。但 [代码](https://github.com/shh-voot/set/blob/e94e1cc70b6f342282eaa9e7d2b15aa84065e381/%E8%A7%A3%E9%A2%98-gpt/%E4%BB%A3%E7%A0%81/solve_problem4_fixed.py#L143) 仍读取问题二，违背题目要求继承问题三全部任务安排和通信关系的条件。

对已交付错峰表逐箱核查：

| 场景 | 硬截止迟到箱数 | 保存的 `on_time` | 超出对应原中继服务窗的任务数 |
|---|---:|---|---:|
| 2 组 | **23** | 全 True | 23 |
| 3 组 | **25** | 全 True | 23 |

两组方案中 S002-WAT-01 和 S002-MED-01 交付时间为 **148.606357 min**，硬截止 **60 min**。这比上一轮的对应时间更晚。迟到箱数变化仅说明组批/分区/时序变化，不代表检查逻辑已经修复。

只保留当前交付分区、改用当前 Q3 时序进行运输资源峰值对照，得到两组 A/B/C 飞机 **5/3/2**、电池 **8/5/4**，三组飞机 **6/4/2**、电池 **8/6/4**。这些数仅用于说明继承差异；因 Q3 已超载且中继不可行，不能作为新的合规配置。

## 5. 新增公式与论文版本的溯源评估

### 5.1 公式编号增加了，来源与实现仍未对应

重点阅读了新 `第三章_问题一正式正文.md`、`第二章第三章_符号公式重排版.md` 及三份新 Word 文档的完整 XML/数学内容。未把普通文本导出中丢失公式视为作者遗漏；文档证据保存在 `evidence/new_papers/`。

| 新公式/论述 | 来源与一致性结论 |
|---|---|
| 式(2.5) 载荷航程 3/2 次幂 | **有原题来源，文字正确**；公共代码仍线性，“严格采用”的执行事实不成立 |
| 式(2.2)–(2.3) 等间隔采样取最大高程 | 是一种离散近似；不能保证得到原题要求的所有经过像元最大值。把它写为“保证”缺证明，已有反例 |
| 式(2.7) `Ecruise=e(q)d` | 与距离成正比可作为建模选择，但 `e(q)` 没有由电池可用能量和航程明确标定；仍不能据式复算结果 |
| 式(2.8) `mgh/(η×3.6×10^6)` | 可由势能与效率定义推导；原题所给 Word 没有展开此子式，需要标注假设和来源。代码实际未实现该式；往返不同载荷、累计正爬升也需分段定义 |
| 式(2.9) 下降附加项 | 本题下降效率为零，数值上不起作用；一般化表达式仍需声明来源，不应误称额外的题给通式 |
| 式(2.13) 自由空间传播 | 理论形式有来源；用 `4πDf/c` 必须约定 D 为 m、f 为 Hz，与原题 MHz/km 常数式统一 |
| 式(2.14) 双向裕度 | 表达式未显式写系统损耗；如打算并入 PL，必须定义。不能遗漏附件的 3 dB 系统损耗 |
| 式(2.15) `LOS=1` 与裕度同时成立 | **新增题意不一致**：原题允许遮挡后加 10 dB，再按预算判链路；遮挡不自动使链路失效。把 LOS 当硬前提会拒绝一部分题目允许的链路 |
| 第三章额外 `2d≤L(q)` | 仍是作者额外的保守限制，没有明确原题独立约束依据；实际去程带载、返程空载，不能无说明替代总能量规则 |
| 第三章宣称 54.1754 kWh 已按新式重算 | 未找到对应运行代码或逐架次表；既与交付 56.515853 不符，也与只改指数的对照 54.181368 不符 |

新增论文同一重排 Word 的摘要写 54.1754 kWh，后文比较段仍写 56.5159 kWh。论文描述 Q1 截止时间排序、FFD、能耗不增的局部移动，与实际程序的组批和最长时间改进也未形成逐项对应。

**本轮仍无法确认所有公式已溯源。** 比上一轮新增的主要风险，是已经写出了看似完整的数学公式和“重新计算”文字，却未将其落实到当前程序及结果。

### 5.2 文献方面的真实改善与限制

新 Word 的 7 条题录比旧 Markdown 的占位清单完整，作者和 arXiv/期刊信息与上轮已核文献基本对应。但没有补齐能耗标定、爬升效率定义和各核心公式的逐式出处。

新增 ALNS 方法说明的 5 篇资料，本轮核对到相应原始来源：

1. Ropke 与 Pisinger（2006）：竞争子启发式按历史表现调整使用频率，是 ALNS 自适应机制的来源。[INFORMS 原文页](https://pubsonline.informs.org/doi/10.1287/trsc.1050.0135)
2. Sacramento、Pisinger、Røpke（2019）：无人机车辆路径 ALNS，题名、作者、卷页与 DOI 对应。[作者所在大学资料页](https://orbit.dtu.dk/en/publications/an-adaptive-large-neighborhood-search-metaheuristic-for-the-vehic/)
3. Goeke 与 Schneider（2015）：混合电动/传统车队路径，考虑速度、坡度及载荷的能耗模型；与题给无人机规则不同。[出版商原文页](https://www.sciencedirect.com/science/article/pii/S0377221715000697)
4. Dönmez、Koç、Altıparmak（2022）：混合车队、部分充电及多充电器的模型与 ALNS，题录可核。[出版商原文页](https://www.sciencedirect.com/science/article/pii/S1366554522002940)
5. Solomon（1987）：时间窗车辆路径经典启发式；能支持问题背景和方法讨论，不能替当前实现证明全部约束满足。[INFORMS 原文页](https://pubsonline.informs.org/doi/10.1287/opre.35.2.254)

这部分没有发现凭空列出的算法文献。应维持新说明中“借鉴搜索框架，不借用外部算例当本题结果”的边界。上述核验是题录、方法概要及实现映射，并未宣称逐行复刻论文算法或验证其全部实验。

## 6. 两套新实验分别评价

### 6.1 `问题2优化/`：ALNS 有有效进展，但验证强度有边界

读取交付候选并独立复核：43 架次、80 箱唯一覆盖，无超重/超容积，无硬迟到；按交付时长与能耗计算的实体机、电池占用无冲突。当前候选仅调整同截止时间层内的执行顺序，未改变原架次的组批、机型和物理路径。

| 指标 | 实际基线 | ALNS 交付候选 |
|---|---:|---:|
| 架次 | 43 | 43 |
| 完成时间 min | 173.727403 | **164.813562** |
| 总运输能耗 kWh | 95.204176 | 95.204176 |
| 硬截止迟到 / 唯一货箱 | 0 / 80 | 0 / 80 |

以文档参数 seed=20260926、250 次迭代在复制目录运行，主要指标复现至浮点误差范围，完成时间减少 **8.913841 min，约 5.13%**。但**具体架次顺序和资源分配表没有完全复现**，37/43 行任务顺序不同；不是单纯的浮点显示差异。当前证据只能确认指标级复现，仍需团队固定生成版本、参数和依赖以闭合逐表复现。

原候选核验器会重新调用同一个 `direct_mission`，所以它验证的是“符合当前程序模型”，无法发现共享的错误航程指数、余量标定和 DEM 问题；它也没有独立覆盖公共布尔量缺失的质量/容积。本轮另外求和后，本候选的质量/容积确实通过，不能把检查器缺项误说成本候选实际超载。

目前保留为实验候选、未直接替换 Q3/Q4 的处理是合理的。不能将上述 5.13% 外推为修正物理模型后仍成立的优化幅度。

### 6.2 `问题2优化claude/`：尚未产出可信完整方案

该目录唯一结果 `帕累托前沿汇总.xlsx` 为 **0 行、0 列**，没有可逐架次核验的优化解。完整大规模遗传搜索没有在本轮执行；以下问题通过原函数、真实附件及小型反例已足以确认。

**问题 A：参数字段与 DEM 接口错误被默认值掩盖。**

- [energy_utils.py:41](https://github.com/shh-voot/set/blob/e94e1cc70b6f342282eaa9e7d2b15aa84065e381/%E8%A7%A3%E9%A2%98-gpt/%E9%97%AE%E9%A2%982%E4%BC%98%E5%8C%96claude/%E4%BB%A3%E7%A0%81/energy_utils.py#L41) 调用不存在的 `dem.get_elevation`，随后 `except: return 0.0`。在真实 S003 路线上返回最高地面为 0 m，巡航海拔变为 50 m，而服务作业海拔已为 338.5 m。
- 读取 `full_load_range_km`，实际 loader 字段为 `full_range_km`，导致三型满载航程全部回落到 **30,000 m**，附件实际为 **20,000 / 16,000 / 12,000 m**。
- 同样存在速度字段不对应及 2 kW 默认功率；并继续线性插值。该函数不应称为已经从“官方代码”正确提取的物理模型。

**问题 B：把完整往返任务函数当作单航段函数。**

[NSGA 评价器](https://github.com/shh-voot/set/blob/e94e1cc70b6f342282eaa9e7d2b15aa84065e381/%E8%A7%A3%E9%A2%98-gpt/%E9%97%AE%E9%A2%982%E4%BC%98%E5%8C%96claude/%E4%BB%A3%E7%A0%81/nsga2_optimizer.py#L192) 遍历 `O→S→O` 的两个边时，每个边都调用含返程、准备和交接的 `direct_mission`，造成重复返程与工序。仅单服务区、C 型运送 S001-MED-01 的测试，现有一次完整任务为 **19.590336 min**，该评价器得到 **38.080672 min**。多点访问也不能这样累加，且所有货箱被统一记为最后一个点交付，未输出真实逐点时刻。

**问题 C：CP-SAT 将持续时间向下截断，产生假可行。**

[`_time_to_int`](https://github.com/shh-voot/set/blob/e94e1cc70b6f342282eaa9e7d2b15aa84065e381/%E8%A7%A3%E9%A2%98-gpt/%E9%97%AE%E9%A2%982%E4%BC%98%E5%8C%96claude/%E4%BB%A3%E7%A0%81/cpsat_scheduler.py#L72) 使用 `int(minutes×10)`。以控制变量的小型合成案例直接调用原求解器：

- 实际送达偏移 1.09 min，截止 1.00 min，程序仍返回 `success=True`；导出记录同时出现 `delivery_min=1.09`、`on_time=False`、`end_min=1.0`。
- 一架飞机执行两个持续 1.09 min 的任务，第二个在 1.0 min 开始，真实重叠 **0.09 min（5.4 秒）**，程序仍返回成功。

这是自建反例，**不冒充已经发生在官方货箱结果中的迟到数量**。它证明该离散化和成功判据不能保证连续时间可行。“CP-SAT 求解器精确”并不意味着传入的截断模型忠于原问题；此外 FEASIBLE 与 OPTIMAL 也不能混为全局最优证明。

## 7. 复现记录与审计限制

| 检查 | 结果 |
|---|---|
| 最新交付 Q1 入口 | DEM 路径错误，`StopIteration`；原 Q1 物理函数及主表仍可逐行复算 |
| 最新交付 Q2 入口 | 成功，43 架次工作簿与交付一致 |
| 最新交付 Q3 入口 | 成功运行，但与已交付表不一致，重跑仍产生超载 |
| 最新交付 Q4 入口 | 成功运行，但与已交付表不一致，仍继承 Q2 |
| 根目录 README 指示的 Q2 入口 | 在配齐原结果目录后成功，产生旧的 45 架次结果 |
| ALNS 250 次实验 | 成功，指标复现，具体调度表不完全一致 |
| 团队 ALNS 校验器 | 成功；存在共享模型错误盲区 |
| 新能耗辅助函数、NSGA 单点评价、CP-SAT 反例 | 均已直接执行，证据在 `additions_summary.json` |

复制环境首次根目录 Q2 运行因未建立输出目录失败，随后补齐复制目录中的空 `结果/` 后重跑成功。这一环境准备问题**不计为团队原程序的新缺陷**；团队仓库本身已有该目录。所有复制程序内容保持原样。

逐架次表中的 `reference_*` 沿用上一轮明确的工程假设：`Ehor=C d/L(q)`、`Eup=mgh/η` 与逐像元取高，只用于敏感性对照，不能宣称找到了原题缺失的唯一能耗公式。**Q3 的超载行超出题给 `0≤q≤Q` 定义域，对这些行的幂函数外推能耗没有合规解释，不作为修正答案使用。**

本轮没有尝试修复或重新优化业务方案，没有长时间运行完整 NSGA-II 搜索，也没有渲染检查全部 Word 页面。新 Word 的公式及数字来自 XML 内容核对；排版是否完全符合规范不在本轮已通过项目内。旧 14 份 PDF 未变化，沿用上一轮题录结论，新增方法文献则另行核验。

## 8. 当前评审意见

**维持“需实质性修订”的结论。** 新增优化实验与论文整理有价值，但没有降低公共物理模型和 Q3 可行性的修订优先级。下一次评估应先看到统一的公式与运行实现、完整执行的中继接替/换电方案、无超载的 Q3，以及从该 Q3 继承而来的 Q4；否则增加算法复杂度和论文公式数量仍不能形成可信终稿。

本报告没有执行任何修复，也没有替换正式方案。上一轮完整评估原样保留；本报告中的新计数和版本判断优先用于解释 `e94e1cc`。

### 附件

- [补充评估明细.xlsx](补充评估明细.xlsx)：旧问题状态、最新逐架次/逐箱、超载、通信、迟到、实验及复现差异。
- [运行与证据说明](README.md)。
- [上一轮报告](../20260925/工作质量与公式溯源审查报告.md)：保留为 `ea5e0fb` 的历史评估。
