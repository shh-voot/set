# 2024华为杯数学建模竞赛D题 - 约束条件详解

## 一、基础约束

### 1.1 载重与体积约束
每架无人机每次飞行必须满足：
```
Σ weight_i ≤ max_payload_kg
Σ volume_i ≤ cargo_volume_m³
```

**具体参数**:
- A型: 25kg, 0.25m³
- B型: 30kg, 0.25m³
- C型: 80kg, 0.25m³ (但S008物资约束为28.9kg)

### 1.2 电池容量约束
每次飞行的能耗不能超过电池容量：
```
E_flight = distance * energy_per_km ≤ battery_capacity
```

**电池容量**:
- A型: 5.03 kWh
- B型: 6.12 kWh
- C型: 8.68 kWh
- R型: 2.47 kWh

### 1.3 飞行距离计算
欧几里得距离：
```python
distance = sqrt((x1-x2)² + (y1-y2)²)
```

### 1.4 能耗计算
```python
# 空载能耗
E_empty = distance_km * energy_empty_per_km

# 满载能耗
E_loaded = distance_km * energy_loaded_per_km

# 实际能耗（线性插值）
load_ratio = current_weight / max_payload
E_actual = E_empty + (E_loaded - E_empty) * load_ratio
```

## 二、充电约束（两阶段模型）

### 2.1 快充阶段（0% → 90%）
- **时间**: 122分钟（固定）
- **恢复电量**: 90% × battery_capacity
- **应用场景**: 需要快速返回任务

### 2.2 慢充阶段（90% → 100%）
- **时间**: 116分钟（固定）
- **恢复电量**: 10% × battery_capacity
- **应用场景**: 长时间待命或最后一批任务

### 2.3 充电决策规则
```python
if remaining_energy < next_trip_requirement:
    if next_trip_requirement <= 90% * battery_capacity:
        charge_to_90%  # 快充122分钟
    else:
        charge_to_100%  # 全充238分钟
```

### 2.4 关键发现
- 快充效率: 90%电量/122分钟 = 0.738%/分钟
- 慢充效率: 10%电量/116分钟 = 0.086%/分钟
- **慢充效率仅为快充的11.7%**

## 三、通信约束

### 3.1 基本要求
所有运输UAV在飞行过程中必须与O01基站或某个中继UAV保持通信连接。

### 3.2 通信距离约束
```
distance(UAV, O01) ≤ 3km  OR
distance(UAV, Relay_i) ≤ 3km for some i
```

### 3.3 中继链路
中继可以形成链路：
```
O01 ← 3km → R1 ← 3km → R2 ← 3km → Service_Area
```

### 3.4 覆盖验证
对每个服务区，必须存在一条通信路径：
```python
def has_communication_path(service_area, relays, O01):
    # BFS/DFS搜索是否存在距离≤3km的连接路径
    return path_exists(O01, service_area, relays, max_hop_distance=3)
```

## 四、时限约束

### 4.1 物资配送时限
每箱物资有独立的配送时限：
```
delivery_time ≤ deadline
```

### 4.2 违约惩罚
赛题中提到"余留比例0.2到0.35时调次品运送"，意味着：
- **必须在时限内完成所有配送**
- **不允许超时或遗漏物资**

## 五、问题二特有约束

### 5.1 决策点分流
- W点: 服务西北片区
- E点: 服务东南片区  
- N点: 服务S004

### 5.2 路径规划
UAV可以从O01先飞到决策点，再从决策点飞到服务区：
```
O01 → Decision_Point → Service_Area → Decision_Point → O01
```

### 5.3 能耗优化
通过决策点分流可以：
- 减少单次飞行距离
- 降低总能耗
- 缩短总时间

## 六、问题三特有约束

### 6.1 中继覆盖要求
- **目标**: 100%覆盖所有15个服务区
- **优化目标**: 最小化中继数量和总能耗

### 6.2 中继能耗计算
中继UAV悬停能耗：
```python
E_relay = hovering_time_hours * hovering_power_kW
```

**参数**:
- 悬停功率: 约0.25 kW
- 悬停时间: 任务时长（假设2小时）
- 单个中继能耗: ≈2.5 kWh

## 七、问题四特有约束

### 7.1 任务区划分
将物资分为三个区域：
- 西北区
- 东南区
- 中继专用区

### 7.2 路径优化
- 西北区从W点分流
- 东南区从E点分流
- 减少O01拥堵

## 八、数据规模

### 8.1 基础数据
- 服务区: 15个
- 物资箱: 80箱
- 决策点: 3个
- UAV类型: 4种

### 8.2 理论最少架次
```
问题一: ≥19架次（根据载重和体积约束）
问题二: ≥30架次（利用决策点分流）
```

### 8.3 能耗范围
```
问题一: 45-60 kWh
问题二: 30-40 kWh（通过分流优化）
问题三: 中继额外15-25 kWh
```

## 九、求解难点

### 9.1 组合优化
- 如何分配80箱物资到最少架次
- 每架次装载哪些货箱

### 9.2 充电调度
- 何时充电（快充vs全充）
- 多UAV并发调度

### 9.3 路径规划
- 决策点选择
- 访问顺序优化

### 9.4 中继部署
- 最优中继位置
- 最少中继数量
- 100%覆盖验证

## 十、我们的创新点

### 10.1 两阶段充电模型
准确实现赛题的快充/慢充机制，避免过度充电浪费时间。

### 10.2 贪心+局部搜索
- 优先级排序（时限、距离、重量）
- First-Fit装箱
- 2-opt路径优化

### 10.3 决策点分流策略
充分利用W、E、N三点，将总能耗从45.28降至31.16 kWh。

### 10.4 中继链路设计
通过R07→R08双跳中继覆盖最远的S008服务区。

