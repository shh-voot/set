# 补充文献调研与三天快速实现方案

**紧急提示**: 只有3天时间，必须采用**快速原型 + 启发式算法**策略，避免过度优化！

---

## 📚 补充核心文献（可快速实现）

### 1. 经典VRP启发式算法（必看！）

#### Clarke-Wright节约算法（1964）
- **用途**: 问题一和问题二的快速初始解
- **优势**: 简单、快速、效果好（通常在最优解15%以内）
- **实现**: 30-50行代码
- **GitHub**: [jnqeras/Nature-will-find-the-way-CVRP](https://github.com/jnqeras/Nature-will-find-the-way-CVRP)

**算法步骤**:
```
1. 初始化：每个服务区一个独立路线
2. 计算节约值 s(i,j) = d(0,i) + d(0,j) - d(i,j)
3. 按节约值降序排列
4. 合并路线（满足容量约束）
```

#### Sweep扫描算法（1974）
- **用途**: 服务区聚类、任务分区（问题四）
- **优势**: 地理位置感知，自然分组
- **实现**: [TrungDuong-Nguyen/Sweep-Heuristic-for-VRP](https://github.com/TrungDuong-Nguyen/Sweep-Heuristic-for-VRP)

**算法步骤**:
```
1. 计算各服务区相对O01的极角
2. 按极角排序
3. 从0度开始扫描，直到达到容量限制
4. 开始新路线，继续扫描
```

### 2. Python现成实现（直接可用！）

#### VRPTW遗传算法实现
**强烈推荐**: [zoopit/py-ga-VRPTW](https://github.com/zoopit/py-ga-VRPTW)
- 完整的Python GA-VRPTW实现
- 包含时间窗约束
- 可直接修改适配本题

**混合GA+PSO实现**: [radoslawik/VRPTW_GA_PSO](https://github.com/radoslawik/VRPTW_GA_PSO)
- 两种算法可切换
- 性能对比

#### Pymoo多目标优化库
**官方文档**: [pymoo NSGA-II tutorial](https://www.pymoo.org/algorithms/moo/nsga2.html)

**快速示例**:
```python
from pymoo.algorithms.moo.nsga2 import NSGA2
from pymoo.core.problem import Problem
from pymoo.optimize import minimize
import numpy as np

class UAVDeliveryProblem(Problem):
    def __init__(self):
        super().__init__(
            n_var=100,      # 决策变量数
            n_obj=4,        # 4个目标
            n_constr=10,    # 约束数
            xl=0, xu=1      # 变量范围
        )
    
    def _evaluate(self, x, out, *args, **kwargs):
        # 目标函数
        f1 = self.calc_timeliness(x)
        f2 = self.calc_completion_time(x)
        f3 = self.calc_energy(x)
        f4 = self.calc_missions(x)
        
        # 约束
        g1 = self.check_capacity(x)
        g2 = self.check_battery(x)
        
        out["F"] = np.column_stack([f1, f2, f3, f4])
        out["G"] = np.column_stack([g1, g2])

# 使用
problem = UAVDeliveryProblem()
algorithm = NSGA2(pop_size=100)
res = minimize(problem, algorithm, ('n_gen', 200))
```

### 3. DEM地形处理（Python实现）

#### Rasterio + Numpy处理DEM
**教程**: [Line-of-Sight Analysis in DEM using Python](https://spatial-dev.guru/2023/12/10/line-of-sight-analysis-in-digital-elevation-models-using-python/)

**高性能实现**: [dunaar/Radio_Lines_Of_Sight](https://github.com/dunaar/Line_Of_Sight)
- 使用Numba加速
- 支持并行计算
- 考虑地球曲率和大气折射

**代码示例**:
```python
import rasterio
import numpy as np

# 读取DEM
with rasterio.open('dem.tif') as src:
    dem = src.read(1)
    transform = src.transform

# 视线检查函数
def check_los(dem, start, end, transform):
    """检查start到end是否视线通畅"""
    # 在start和end之间采样点
    n_samples = 100
    x_coords = np.linspace(start[0], end[0], n_samples)
    y_coords = np.linspace(start[1], end[1], n_samples)
    
    # 提取高程
    elevations = []
    for x, y in zip(x_coords, y_coords):
        row, col = ~transform * (x, y)
        elevations.append(dem[int(row), int(col)])
    
    # 检查遮挡
    start_elev = elevations[0] + start[2]  # 起点高度
    end_elev = elevations[-1] + end[2]     # 终点高度
    
    for i, terrain_elev in enumerate(elevations[1:-1], 1):
        # 直线插值高度
        t = i / (n_samples - 1)
        line_height = start_elev * (1-t) + end_elev * t
        
        if terrain_elev > line_height:
            return False  # 被遮挡
    
    return True  # 通畅
```

### 4. 多维背包问题启发式

**Bin Completion算法**: [ArXiv Paper](http://arxiv.org/pdf/1110.2209v1)
- 适用于多容器、多约束背包问题
- 启发式+局部搜索

**First Fit Decreasing (FFD)**:
```python
def pack_boxes(boxes, max_weight, max_volume):
    """FFD算法打包货箱"""
    # 按重量降序排序
    sorted_boxes = sorted(boxes, key=lambda x: x.weight, reverse=True)
    
    batches = []
    current_batch = []
    current_weight = 0
    current_volume = 0
    
    for box in sorted_boxes:
        if (current_weight + box.weight <= max_weight and 
            current_volume + box.volume <= max_volume):
            current_batch.append(box)
            current_weight += box.weight
            current_volume += box.volume
        else:
            if current_batch:
                batches.append(current_batch)
            current_batch = [box]
            current_weight = box.weight
            current_volume = box.volume
    
    if current_batch:
        batches.append(current_batch)
    
    return batches
```

### 5. 电池充电模型（实用参考）

**两阶段充电模型论文**: 
- [An Aging-Optimized MCC Fast Charging Algorithm](https://www.mdpi.com/2313-0105/10/8/267)
- [SOC Estimation Optimization](https://www.mdpi.com/2079-9292/14/7/1462)

**简化实现**:
```python
class Battery:
    def __init__(self, capacity_kwh, full_charge_time_min):
        self.capacity = capacity_kwh
        self.full_charge_time = full_charge_time_min
        self.soc = 1.0  # 初始100%
    
    def get_charge_time(self, from_soc):
        """计算从from_soc充到100%需要的时间（分钟）"""
        if from_soc >= 0.90:
            # 慢充阶段
            return self.full_charge_time * 0.35 * (1 - from_soc) / 0.10
        else:
            # 快充阶段 + 慢充阶段
            fast_time = self.full_charge_time * 0.65 * (0.90 - from_soc) / 0.90
            slow_time = self.full_charge_time * 0.35
            return fast_time + slow_time
    
    def can_fly_mission(self, energy_required, safety_margin=0.2):
        """检查是否有足够电量执行任务"""
        available = self.soc * self.capacity
        required = energy_required / (1 - safety_margin)
        return available >= required
```

### 6. 中国数学建模竞赛案例

#### 相关竞赛题目参考
- **2019华为杯F题**: [GitHub代码](https://github.com/QY1996/2019F) - 获二等奖完整方案
- **2020河北杯F题**: [无人机侦听与反制](https://github.com/jaykwok/HeBeiCup2020-F)
- **农村公交与异构无人机协同配送**: [腾讯云文章](https://developer.cloud.tencent.com/article/2428118)

**关键启发**:
1. 竞赛重视**建模合理性**而非算法复杂性
2. **分阶段求解**：先简化模型，再逐步完善
3. **可视化结果**非常重要：路线图、甘特图、统计表
4. **敏感性分析**增加论文深度

---

## ⚡ 三天快速实现方案

### 时间分配（72小时）

#### Day 1（今天）：基础框架 + 问题一（24小时）

**上午（4小时）**: 数据加载与验证
```python
# 立即实现的模块
1. 读取所有Excel文件
2. 验证数据完整性
3. 可视化服务区分布图
4. 计算基础距离矩阵
```

**下午（4小时）**: 能耗与DEM模块
```python
1. 实现能耗计算函数（题目公式）
2. 读取DEM数据
3. 计算地形净空
4. 预计算能耗矩阵
```

**晚上（4小时）**: 问题一求解
```python
1. 实现FFD货箱组批算法
2. 计算每种机型的最大安全载荷
3. 生成问题一结果表格
4. 敏感性分析（返航余量）
```

**深夜（可选2-4小时）**: 完善问题一可视化

---

#### Day 2（明天）：问题二 + 问题三基础（24小时）

**上午（6小时）**: 问题二框架
```python
1. 实现Clarke-Wright算法生成初始解
2. 搭建电池管理系统
3. 验证约束检查函数
```

**下午（6小时）**: NSGA-II优化
```python
1. 定义问题类（继承pymoo.Problem）
2. 实现编码/解码函数
3. 运行NSGA-II（至少100代）
4. 提取Pareto前沿
```

**晚上（6小时）**: 问题二结果
```python
1. 选择推荐解
2. 生成详细调度表
3. 绘制甘特图
4. 验证资源可行性
```

**深夜（可选）**: 问题二优化调参

---

#### Day 3（后天）：问题三 + 问题四 + 论文（24小时）

**上午（4小时）**: 通信模块
```python
1. 实现链路预算计算
2. 视线遮挡检查（简化版）
3. 识别通信盲区
```

**上午-中午（4小时）**: 中继优化
```python
1. 贪心算法选择中继位置
2. 时空调度（简单规则）
3. 生成问题三结果
```

**下午（4小时）**: 问题四
```python
1. Sweep算法分区（2组和3组）
2. 统计资源需求
3. 对比分析
```

**下午-晚上（8小时）**: 论文撰写
```
1. 模型假设与建立
2. 算法设计
3. 结果分析
4. 敏感性分析
5. 模型评价
```

**深夜（4小时）**: 最后检查与提交准备

---

## 🔧 必备Python库（立即安装）

```bash
# 基础库
pip install numpy pandas openpyxl matplotlib

# 优化库
pip install pymoo scipy

# 地理空间
pip install rasterio geopandas shapely

# 可视化
pip install plotly seaborn

# 加速（可选）
pip install numba
```

---

## 📋 代码框架（今天就开始）

### 项目结构
```
D题/
├── data/              # 数据文件
├── src/
│   ├── data_loader.py      # 数据加载
│   ├── energy_model.py     # 能耗计算
│   ├── dem_processor.py    # DEM处理
│   ├── battery_manager.py  # 电池管理
│   ├── communication.py    # 通信模型
│   ├── problem1.py         # 问题一
│   ├── problem2.py         # 问题二（NSGA-II）
│   ├── problem3.py         # 问题三
│   ├── problem4.py         # 问题四
│   └── visualization.py    # 可视化
├── results/           # 结果输出
├── figures/           # 图表
└── main.py           # 主程序
```

### 今天就写的核心代码

#### data_loader.py
```python
import pandas as pd
import numpy as np

class DataLoader:
    def __init__(self, data_dir):
        self.data_dir = data_dir
    
    def load_all(self):
        """加载所有数据"""
        # 调度中心与服务区
        self.centers = pd.read_excel(
            f'{self.data_dir}/无人机应急物资运输基础数据/调度中心与服务区.xlsx'
        )
        
        # 货箱需求
        self.demands = pd.read_excel(
            f'{self.data_dir}/无人机应急物资运输基础数据/物资需求与配送时限.xlsx'
        )
        
        # 无人机参数
        self.drones = pd.read_excel(
            f'{self.data_dir}/无人机应急物资运输基础数据/运输无人机数据.xlsx'
        )
        
        # 中继无人机
        self.relays = pd.read_excel(
            f'{self.data_dir}/无人机应急物资运输基础数据/中继无人机数据.xlsx'
        )
        
        # 通信参数
        self.comm_params = pd.read_excel(
            f'{self.data_dir}/无人机应急物资运输基础数据/通信链路参数.xlsx'
        )
        
        print("✓ 数据加载完成")
        return self
    
    def get_distance_matrix(self):
        """计算距离矩阵"""
        n = len(self.centers)
        dist = np.zeros((n, n))
        
        for i in range(n):
            for j in range(n):
                dx = self.centers.loc[i, '经度'] - self.centers.loc[j, '经度']
                dy = self.centers.loc[i, '纬度'] - self.centers.loc[j, '纬度']
                # 简化：平面距离（后续可改为大圆距离）
                dist[i, j] = np.sqrt(dx**2 + dy**2) * 111000  # 转为米
        
        return dist
```

#### energy_model.py
```python
import numpy as np

class EnergyModel:
    def __init__(self, drone_params):
        self.params = drone_params
    
    def equivalent_range(self, model, payload):
        """等效航程公式（题目附录2）"""
        L0 = self.params[model]['空载航程']
        LF = self.params[model]['满载航程']
        Q = self.params[model]['最大载重']
        
        if payload > Q:
            return 0
        
        L = L0 - (L0 - LF) * (payload / Q) ** 1.5
        return L
    
    def flight_time(self, model, h_up, d_horiz, h_down):
        """飞行时间（秒）"""
        v_up = self.params[model]['爬升速度']
        v_cruise = self.params[model]['巡航速度']
        v_down = self.params[model]['下降速度']
        
        t = h_up / v_up + d_horiz / v_cruise + h_down / v_down
        return t
    
    def energy_consumption(self, model, h_up, d_horiz, payload):
        """能耗计算（kWh）"""
        # 水平巡航能耗
        L_eq = self.equivalent_range(model, payload)
        E_horiz = (d_horiz / L_eq) * self.params[model]['电池容量']
        
        # 爬升附加能耗（简化）
        climb_power = self.params[model]['爬升功率']
        v_up = self.params[model]['爬升速度']
        E_climb = (h_up / v_up) * climb_power / 3600  # 转kWh
        
        return E_horiz + E_climb
    
    def max_safe_payload(self, model, h_up, d_horiz, h_down, safety_margin=0.2):
        """最大安全载荷（二分搜索）"""
        Q = self.params[model]['最大载重']
        E_avail = self.params[model]['电池容量'] * (1 - safety_margin)
        
        # 二分搜索
        left, right = 0, Q
        result = 0
        
        while right - left > 0.1:
            mid = (left + right) / 2
            E_required = self.energy_consumption(model, h_up, d_horiz, mid) * 2  # 往返
            
            if E_required <= E_avail:
                result = mid
                left = mid
            else:
                right = mid
        
        return result
```

---

## 🎯 关键成功要素

### 1. 时间分配原则
- **60%写代码**: 实现基础功能
- **20%调试**: 验证正确性
- **20%论文**: 模型说明与结果分析

### 2. 简化策略（在不影响合理性前提下）
- ✅ 使用启发式算法（Clarke-Wright, Sweep, Greedy）
- ✅ 视线检查采样而非精确计算
- ✅ NSGA-II运行100-200代即可
- ✅ 问题三可用迭代2-3轮

- ❌ 不要尝试精确求解（整数规划会超时）
- ❌ 不要过度调参（够用就行）
- ❌ 不要实现所有文献算法

### 3. 必须完成的验证
1. 所有约束满足（载重、体积、能量、时间窗）
2. 资源不冲突（同一无人机/电池不能同时用）
3. 充电时间合理
4. 通信链路可用

### 4. 论文撰写技巧
- **模型假设**要明确（哪些简化了）
- **算法流程图**清晰
- **结果表格**完整（Excel提交模板）
- **可视化**直观（路线图、甘特图、Pareto前沿）
- **敏感性分析**展示稳定性

---

## 📚 最终参考文献列表（精选20篇）

### 基础理论
1. [Dorling et al. (2017) - Vehicle Routing Problems for Drone Delivery](https://ieeexplore.ieee.org/document/7523711)
2. [Zhang et al. (2021) - Energy Consumption Models](https://www.sciencedirect.com/science/article/pii/S1361920920308361)

### 算法实现
3. [Clarke-Wright Savings Algorithm](http://science.utm.my/procscimath/wp-content/uploads/sites/605/2022/10/101-109-Khoo-Xu-Yi_Farhana-Johar.pdf)
4. [Sweep Algorithm for VRP](https://metricgate.com/docs/sweep-algorithm-vrp/)
5. [GitHub: py-ga-VRPTW Implementation](https://github.com/zoopit/py-ga-VRPTW)
6. [GitHub: VRPTW GA+PSO Hybrid](https://github.com/radoslawik/VRPTW_GA_PSO)

### 多目标优化
7. [Pymoo NSGA-II Documentation](https://www.pymoo.org/algorithms/moo/nsga2.html)
8. [Multi-Objective UAV-Rider Delivery](https://www.mdpi.com/2226-4310/13/8/673)
9. [Immune-enhanced NSGA-II for Disaster Relief](https://www.frontiersin.org/articles/10.3389/ffutr.2026.1801962)

### 地形与通信
10. [Line-of-Sight Analysis in DEM using Python](https://spatial-dev.guru/2023/12/10/line-of-sight-analysis-in-digital-elevation-models-using-python/)
11. [GitHub: Radio Lines of Sight](https://github.com/dunaar/Line_Of_Sight)
12. [UAV Communication Bridges for Emergency](https://arxiv.org/pdf/2609.02562)
13. [AoI Optimization through Relay Selection](https://arxiv.org/html/2505.09386v1)

### 电池管理
14. [Optimizing UAV Recharge Scheduling](https://arxiv.org/html/2205.12656v1)
15. [Battery Swapping Real-Time Scheduling](https://www.mdpi.com/1996-1073/18/9/2193)
16. [Two-Stage Charging Model](https://www.mdpi.com/2313-0105/10/8/267)

### 背包与装箱
17. [Bin Completion for Multiple Constraints](http://arxiv.org/pdf/1110.2209v1)
18. [Multidimensional Knapsack Heuristics](https://arxiv.org/html/2111.05377v2)

### 竞赛案例
19. [2019华为杯研究生数学建模F题](https://github.com/QY1996/2019F)
20. [农村公交与异构无人机协同配送](https://developer.cloud.tencent.com/article/2428118)

---

## ✅ 行动清单（立即开始！）

### 今天晚上必须完成：
- [ ] 安装所有Python库
- [ ] 读取并验证所有数据文件
- [ ] 实现能耗计算模块
- [ ] 实现问题一的FFD组批算法
- [ ] 生成问题一初步结果

### 明天必须完成：
- [ ] Clarke-Wright初始解
- [ ] NSGA-II框架搭建
- [ ] 电池调度模块
- [ ] 问题二完整结果

### 后天必须完成：
- [ ] 问题三和问题四
- [ ] 完整论文
- [ ] 结果验证
- [ ] 提交准备

---

**现在立即开始编码！不要再搜文献了！**
