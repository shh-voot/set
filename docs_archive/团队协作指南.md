# 华为杯D题 - 团队协作指南

## 📋 项目信息

- **GitHub仓库**: https://github.com/shh-voot/set
- **项目名称**: 山区洪涝灾害下无人机运输与通信协同优化
- **团队**: 华为杯参赛队

---

## 🚀 新成员加入指南

### 1. 获取协作权限

**项目负责人操作**：
1. 访问：https://github.com/shh-voot/set/settings/access
2. 点击 **Add people**（添加人员）
3. 输入团队成员的GitHub用户名或邮箱
4. 发送邀请

**团队成员操作**：
1. 检查邮箱，点击邀请链接
2. 或访问：https://github.com/shh-voot/set
3. 接受邀请

### 2. 克隆项目到本地

```bash
# 克隆仓库
git clone https://github.com/shh-voot/set.git

# 进入项目目录
cd set

# 配置你的Git信息（只需第一次）
git config user.name "你的名字"
git config user.email "你的邮箱"
```

### 3. 安装依赖

```bash
# 安装Python依赖
pip install -r requirements.txt

# 测试运行
python read_data.py
```

---

## 💻 日常工作流程

### ⭐ 每次开始工作前（重要！）

```bash
# 1. 进入项目目录
cd "D:/claude/华为杯/D题"

# 2. 拉取最新代码（避免冲突）
git pull origin main

# 3. 查看当前状态
git status
```

### ✏️ 修改代码

```bash
# 1. 修改你的文件...
# 2. 随时查看修改了什么
git status

# 3. 查看具体改了哪些内容
git diff
```

### 📤 提交并同步给团队

```bash
# 1. 添加修改的文件
git add .                          # 添加所有修改
# 或
git add 具体文件.py                # 只添加特定文件

# 2. 提交修改（写清楚改了什么）
git commit -m "简短描述：比如'优化问题二的NSGA-II算法'"

# 3. 推送到GitHub（同步给团队）
git push origin main

# 如果提示冲突，先拉取：
git pull origin main
# 解决冲突后再push
```

---

## 📝 提交信息规范

### 好的提交信息示例 ✅

```bash
git commit -m "修复问题一能耗计算公式错误"
git commit -m "添加问题三中继位置可视化图表"
git commit -m "更新README，补充安装步骤"
git commit -m "优化问题二电池充电模型，提高准确性"
git commit -m "完成问题四批次划分算法实现"
```

### 不好的提交信息 ❌

```bash
git commit -m "修改"           # 太模糊
git commit -m "update"         # 看不懂改了什么
git commit -m "aaa"            # 无意义
git commit -m "临时保存"       # 不专业
```

### 提交信息模板

```
[类型] 简短描述

类型可以是：
- 修复: 修复bug
- 添加: 添加新功能
- 优化: 改进性能或代码
- 文档: 更新文档
- 重构: 重构代码
- 测试: 添加测试

示例：
git commit -m "修复: 问题二电池SOC计算错误"
git commit -m "添加: 问题三通信覆盖热力图"
git commit -m "优化: 改进NSGA-II收敛速度"
```

---

## 🔄 常见操作

### 查看提交历史

```bash
# 查看最近5次提交
git log --oneline -5

# 查看详细提交信息
git log -3

# 查看某个文件的修改历史
git log --oneline 文件名.py
```

### 查看团队成员的修改

```bash
# 查看最新的修改
git pull origin main

# 查看谁改了什么
git log --oneline --all --graph -10
```

### 撤销修改

```bash
# 撤销还未提交的修改
git checkout -- 文件名.py

# 撤销最近一次提交（但保留修改）
git reset --soft HEAD~1

# 查看某个历史版本
git checkout 提交号 文件名.py
```

---

## ⚠️ 遇到冲突怎么办

### 场景：push时提示冲突

```bash
$ git push origin main
To https://github.com/shh-voot/set.git
 ! [rejected]        main -> main (fetch first)
error: failed to push some refs
```

### 解决步骤：

```bash
# 1. 先拉取远程最新代码
git pull origin main

# 2. Git会提示哪些文件有冲突，打开这些文件
# 你会看到类似这样的标记：

<<<<<<< HEAD
你的代码
=======
队友的代码
>>>>>>> origin/main

# 3. 手动编辑文件，保留正确的代码，删除标记

# 4. 标记冲突已解决
git add 冲突文件.py

# 5. 完成合并
git commit -m "解决与XXX的合并冲突"

# 6. 推送
git push origin main
```

---

## 👥 团队分工建议

### 按问题分工

```
├── 成员A: 问题一 + 问题二
│   └── 文件: main_problem1.py, solve_problem2.py
│
├── 成员B: 问题三 + 问题四  
│   └── 文件: solve_problem3.py, solve_problem4.py
│
├── 成员C: 可视化 + 文档
│   └── 文件: 问题*_可视化.py, README.md
│
└── 成员D: 论文撰写 + 整体审查
    └── 文件: 论文.docx, 结果分析
```

### 按模块分工

```
├── 成员A: 数据处理 + 能耗模型
│   └── src/data/, src/energy/
│
├── 成员B: 优化算法 + 求解器
│   └── solve_*.py, src/problem1/
│
├── 成员C: 地形 + 通信
│   └── src/terrain/, src/communication/
│
└── 成员D: 可视化 + 文档
    └── 问题*_可视化.py, 文档/
```

---

## 📅 协作最佳实践

### 1. 经常同步

```bash
# 建议：每次开始工作前拉取，每完成一个小功能就提交
git pull origin main    # 开始工作前
# ... 工作1-2小时 ...
git add .
git commit -m "完成XX功能"
git push origin main    # 及时推送
```

### 2. 小步提交

```bash
# 好的做法：每完成一个小功能就提交
git commit -m "完成能耗计算函数"
git commit -m "添加电池管理类"
git commit -m "完成NSGA-II编码方案"

# 不好的做法：攒了一天的修改一次提交
git commit -m "完成所有代码"  # ❌ 太大了
```

### 3. 沟通协调

- 📱 **工作前**: 在群里说一声你要改哪个文件
- 💬 **遇到问题**: 及时在群里讨论
- ✅ **完成后**: 推送后通知队友拉取最新代码

---

## 🛠️ 常用命令速查表

```bash
# 基础操作
git status              # 查看当前状态
git pull origin main    # 拉取最新代码
git add .               # 添加所有修改
git commit -m "信息"    # 提交修改
git push origin main    # 推送到GitHub

# 查看操作
git log --oneline -5    # 查看最近5次提交
git diff                # 查看修改内容
git diff 文件名         # 查看某文件的修改

# 撤销操作
git checkout -- 文件名  # 撤销未提交的修改
git reset --soft HEAD~1 # 撤销最近一次提交

# 分支操作（高级）
git branch              # 查看分支
git checkout -b 新分支  # 创建新分支
git merge 分支名        # 合并分支
```

---

## 🎯 注意事项

### ✅ 应该做的：

1. **每次工作前先pull**
2. **提交信息写清楚**
3. **经常提交，及时推送**
4. **遇到冲突不要慌，仔细解决**
5. **重要修改前先备份**

### ❌ 不应该做的：

1. ~~直接修改main分支的历史提交~~
2. ~~使用`git push --force`（除非确定）~~
3. ~~提交密码、个人信息~~
4. ~~提交超大文件（>100MB）~~
5. ~~长时间不拉取就推送~~

---

## 🆘 遇到问题？

### 常见问题

**Q1: 为什么我看不到.git文件夹？**
```bash
# Windows资源管理器显示隐藏文件：
# 查看 → 选项 → 查看 → 显示隐藏的文件、文件夹和驱动器

# 或在Git Bash中查看：
ls -la
```

**Q2: push被拒绝怎么办？**
```bash
# 先拉取，解决冲突，再推送
git pull origin main
git push origin main
```

**Q3: 我改错了怎么恢复？**
```bash
# 如果还没提交
git checkout -- 文件名

# 如果已经提交但没push
git reset --soft HEAD~1

# 如果已经push了，找队长帮忙
```

**Q4: 如何查看队友改了什么？**
```bash
git pull origin main
git log --oneline -5
git show 提交号
```

---

## 📞 团队联系方式

| 角色 | 姓名 | 分工 | 联系方式 |
|------|------|------|----------|
| 队长 | XXX | 整体协调 | 微信/QQ |
| 成员A | XXX | 问题一、二 | 微信/QQ |
| 成员B | XXX | 问题三、四 | 微信/QQ |
| 成员C | XXX | 可视化 | 微信/QQ |

---

## 🏆 项目目标

- **完成时间**: 竞赛截止日期前
- **目标成绩**: 一等奖
- **核心任务**: 
  - ✅ 4个问题全部完成
  - ✅ 论文撰写完成
  - ✅ 可视化图表完成
  - ✅ 代码测试通过

---

**记住：经常pull，及时commit，写清楚改了什么！** 🚀

**GitHub仓库**: https://github.com/shh-voot/set
